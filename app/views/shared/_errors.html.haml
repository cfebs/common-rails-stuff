-# locals: error from ApplicationHelper::errors_for
-#         flash from ApplicationHelper::flash_zone 

#error_explanation
  %h2= "#{pluralize(object.errors.count, "error")} prohibited this post from being saved:"
  %ul
    - object.errors.full_messages.each do |msg|
      %li= msg
